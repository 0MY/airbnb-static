<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="UTF-8">
    <title>Team</title>
  </head>

  <body>
    <ul class="list-inline text-center">
      <% ["Olivier", "Philippe"].each do |member| %>
        <li>
          <%= image_tag "#{member}.jpeg", class: "img-circle" %>
          <h3><%= member.capitalize %></h3>
        </li>
      <% end %>
    </ul>
  </body>

</html>
